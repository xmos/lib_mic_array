#include <xs1.h>
#include <stdint.h>
#include "double_buffer.h"

int process_buffer(BUFFER_TYPE *buffer) {
  for(int i=0; i<BUF_SIZE; i++) {
     //printf("output buffer value: 0x%x\n", buffer[i]);
  }
  return 0;
}
